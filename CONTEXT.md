# Context

Domain glossary. Only terms non-obvious from the code are listed; this is preliminary and refined by `/domain-modeling` as terms get resolved.

(No non-obvious domain terms yet — the three package names `list_utils`, `math_utils`, `string_utils` are self-descriptive and carry no implementation to derive further vocabulary from. Add entries here as functions are introduced and concepts solidify.)
