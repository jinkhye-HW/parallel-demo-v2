# Context

Domain glossary. Terms non-obvious from the code.

The repo is a skeleton — no domain terms are established yet. The three module names (`math_utils`, `string_utils`, `list_utils`) are self-descriptive and need no glossary entries at this stage.

This file is preliminary. The `domain-modeling` skill refines and extends it as real domain language emerges during implementation.
