# parallel-demo

Utility modules for math, string, and list operations.

## Packages

- `math_utils` — math utility functions
- `string_utils` — string utility functions
- `list_utils` — list utility functions

## Install

```bash
pip install -e .[dev]
```

## Test

```bash
pytest
```
